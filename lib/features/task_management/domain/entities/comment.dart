class Comment {
  final String? id;
  final String ?content;
  final String? projectId;
  final String? taskId;

  const Comment({
     this.id,
     this.content,
     this.projectId,
     this.taskId,
  });
}